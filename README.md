#  Crop Yield Prediction - Backend

This is the backend API for predicting crop yields using machine learning.  
It uses **Flask**, **Scikit-learn**, and a trained regression model.

---

##  Setup Instructions

1. Clone or download this project.
2. Open terminal inside the folder and run:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
